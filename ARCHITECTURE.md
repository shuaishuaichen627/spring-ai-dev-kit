# 架构设计文档

## 系统架构

### 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                      用户层                              │
│              (Web UI / API Client)                      │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                    接入层 (Nginx)                        │
│              负载均衡 / 反向代理 / SSL                   │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                  应用层 (Spring Boot)                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │  Agent   │  │   RAG    │  │   MCP    │              │
│  │  Module  │  │  Module  │  │  Module  │              │
│  └──────────┘  └──────────┘  └──────────┘              │
│         ↓             ↓             ↓                    │
│  ┌─────────────────────────────────────┐                │
│  │        Common Module                │                │
│  └─────────────────────────────────────┘                │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                    数据层                                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │PostgreSQL│  │  Redis   │  │  Ollama  │              │
│  │+PGVector │  │          │  │          │              │
│  └──────────┘  └──────────┘  └──────────┘              │
└─────────────────────────────────────────────────────────┘
```

## 核心模块设计

### Agent 模块

**职责**：智能体编排，协调 RAG 和 MCP 能力

**核心类**：
- `AgentService`: 核心服务，处理用户请求
- `AgentController`: REST 接口
- `ChatRequest`: 请求 DTO

**流程**：
1. 接收用户消息
2. 调用 RAG 检索相关知识
3. 判断是否需要调用 MCP 工具
4. 构建提示词
5. 调用大模型
6. 流式返回结果

### RAG 模块

**职责**：知识库管理，文档解析，向量检索

**核心类**：
- `GitParserService`: Git 代码解析
- `DocumentParserService`: 文档解析
- `VectorStoreService`: 向量库操作
- `RagController`: REST 接口

**流程**：
1. 解析文档/代码
2. 文本分块
3. 向量化
4. 存储到 PGVector
5. 相似度检索

### MCP 模块

**职责**：工具管理，工具调度

**核心类**：
- `McpTool`: 工具接口
- `McpGatewayService`: 工具网关
- `ElkLogTool`: ELK 工具实现
- `PrometheusTool`: Prometheus 工具实现

**扩展方式**：
实现 `McpTool` 接口，Spring 自动注册

## 数据流

### RAG 数据流

```
文档/代码 → 解析 → 分块 → 向量化 → PGVector
                                      ↓
用户查询 → 向量化 → 相似度检索 ← PGVector
```

### Agent 数据流

```
用户消息 → Agent → RAG 检索 → 构建上下文
                ↓
              MCP 工具调用
                ↓
            大模型推理
                ↓
            SSE 流式返回
```

## 技术选型

| 组件 | 技术 | 理由 |
|------|------|------|
| 应用框架 | Spring Boot 3.2 | 成熟稳定，生态丰富 |
| AI 框架 | Spring AI 1.0 | 官方支持，易于集成 |
| 向量数据库 | PGVector | 开源免费，性能优秀 |
| 大模型 | Ollama/OpenAI | 本地部署 + 云端备选 |
| 缓存 | Redis | 高性能，支持多种数据结构 |
| 容器化 | Docker | 标准化部署 |

## 性能优化

### 向量检索优化
- 使用 HNSW 索引
- 合理设置 topK 参数
- 缓存热门查询结果

### 大模型优化
- 流式响应减少等待时间
- 合理设置 temperature 参数
- 控制上下文长度

### 缓存策略
- Redis 缓存向量检索结果
- 缓存 MCP 工具执行结果
- 设置合理的过期时间

## 安全设计

### 认证授权
- JWT Token 认证
- RBAC 权限控制
- API 限流

### 数据安全
- 敏感信息加密存储
- SQL 注入防护
- XSS 防护

## 监控告警

### 监控指标
- 接口响应时间
- 大模型调用次数
- 向量检索性能
- 系统资源使用

### 日志
- 结构化日志
- ELK 日志分析
- 错误追踪

## 扩展性

### 水平扩展
- 无状态设计
- 负载均衡
- 分布式缓存

### 功能扩展
- 插件化 MCP 工具
- 可配置的 RAG 策略
- 多模型支持

